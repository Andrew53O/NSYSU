    // for (em_records data : data_employee) // data have the same type as data_employee maybe
    // {
    //     double total = calculate(data.core_competency, data.performance_evaluation, data.ideation, data.result_evaluation);
    //     cout << index << setw(5) << " " << total << endl; // << point(some_var)
    //     index++;
    // }
